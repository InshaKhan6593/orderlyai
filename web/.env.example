# OrderlyAI web (Next.js) environment.
# Copy to .env.local for local development. Only NEXT_PUBLIC_* vars reach the browser.

# Base URL of the FastAPI backend (api/). Includes the /api/v1 prefix.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
